package tech.wenisch.proxera.config;

// AdminServerConfig is intentionally empty.
// The second Tomcat connector has been removed; admin UI and proxy now share the
// single connector on server.port (default 8080).
public class AdminServerConfig {
}
