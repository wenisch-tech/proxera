Proxera {{ .Chart.AppVersion }} has been deployed.

Service port: {{ .Values.service.port }}

Access the proxy via port-forward (or configure the wildcard ingress via the Topology UI):
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "proxera.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}

{{- if .Values.ingress.admin.enabled }}
Admin URL:
{{- range .Values.ingress.admin.hosts }}
  http{{ if $.Values.ingress.admin.tls }}s{{ end }}://{{ .host }}/admin
{{- end }}
{{- else }}
Access the Admin UI via port-forward:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "proxera.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
  Open: http://localhost:{{ .Values.service.port }}/admin
{{- end }}

Default credentials: admin@proxera.local / admin
WARNING: Change the default password immediately after first login.
